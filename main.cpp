#include <iostream>

using namespace std;

int main()
{
	double k,i,j;
	float dim;
	char messaggio[dim];
	char tabella[k][j];
	char lettera;
	cin>>dim;
	for (i=0;i<dim;i++)
	{
		cin>>lettera;
		lettera=messaggio[i];
	}
	for (i=0;i<dim;i++)
	     cout<<messaggio[i];
	return 0;
}
